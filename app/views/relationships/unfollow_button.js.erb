$('#unfollow_user').bind('ajax:success', function(){
   $(this).closest('#unfollow_user').remove();
   $('#follow-container').append('<%= link_to "Follow", relationships_path(id: user.id), remote: true, class:"button follow", id: "follow_user", method: :post %>');
});