$('#unfollow_user').bind('ajax:success', function(){
   $(this).closest('#unfollow_user').hide();
   $(this).closest('#follow-container').html('<%= link_to "Follow", relationships_path(id: user.id), remote: true, class:"button follow", id: "follow_user", method: :post %>');
});